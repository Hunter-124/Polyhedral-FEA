# 0005 — Data card: advisor training corpus

Companions: [0004 — model card](0004-model-card.md),
[0003 — training log](0003-training-log.md),
[ADR-0027](../decisions/0027-learned-mesh-advisor.md).

> **Status.** All 72 reference truths were replaced with an independent
> Gmsh + CalculiX chain after the measurements below were taken, and the campaign
> is being regenerated on a corrected engine with a re-aimed action grid. Row
> counts and label distributions will change. What will not change is the
> *structure* documented here, which is what the caveats are about.

## Provenance

| | |
|---|---|
| Geometry | procedural, generated by `scripts/gen_primitive_corpus.py` from committed literal seeds via OpenCASCADE (OCP) |
| Cases | 3 load archetypes per part — axial tension, transverse bending, oblique |
| Rows | one per (case, meshing action), emitted by `apps/testlab` as schema `advisor-row-v3` |
| Assembly | `scripts/build_advisor_dataset.py` → `bench/advisor/dataset.csv` |
| Descriptors | `scripts/advisor/geometry_features.py` → `bench/advisor/geometry_features.csv`, computed offline from the STEP files |
| Licence | entirely self-generated. No third-party CAD corpus is included, so there is no licence union to track. ADR-0027 §6 governs any future external corpus; SimJEB and the Fusion 360 Gallery are non-commercial and must not touch a shipped checkpoint |

## Composition

8 families × 4 size regimes × 3 load cases = **96 cases**.

| family | shape | notes |
|---|---|---|
| `box_hole` | plate with a central through-hole | Kirsch/Howland analytic reference |
| `l_bracket` | two fused prismatic legs | re-entrant corner |
| `plate_notch` | plate with opposed edge notches | |
| `stepped_shaft` | two coaxial cylinders | analytic cantilever reference |
| `channel` | thin-walled U section | thin wall, prismatic |
| `sphere_box` | box with a spherical boss | curved loaded surface |
| `tube` | hollow circular section | **new**: curved *and* thin-walled |
| `perforated_plate` | plate with 3–4 through-holes | **new**: smallest features in the corpus |

The last two were added for a *power* test, not for coverage: at six families
the effect of adding a family on held-out regret is unmeasurable (slope 95 % CI
[−0.051, +0.043] decades per family), and eight families is the point at which a
0.02-decade effect becomes detectable.

**Caveat on `perforated_plate`, stated because it weakens the test.** Its
standardized descriptor centroid sits 1.70 from `plate_notch`, *below* the
existing corpus minimum pairwise distance of 2.22 — it is the closest pair in the
corpus. `tube` is genuinely distinct (3.18 from `stepped_shaft`). So
`perforated_plate` is a **weaker test of transfer** than `tube`, and a flat slope
at eight families should be reported with that qualification rather than as a
clean falsification. `t_junction` or `lug` is the obvious next test.

## The split — read this before quoting any validation number

**The corpus is parametric.** `box_hole_s0_c0` and `box_hole_s0_c1` are the
*same CAD solid* under a different load case. A per-row or per-part split
therefore puts near-duplicates, and in this corpus exact duplicates, on both
sides. Measured on the v3 corpus under the historical part-hash split:

- 11 of 14 validation cases had their exact geometry present in training under a
  different BC index;
- 6 of 6 families were on both sides;
- **672 of 672 validation rows had a training row with an identical
  (geometry-feature, action) vector.** Only the 7 `case_*` BC columns differed.

Every validation MAE and every regret number published before this was found was
measuring interpolation across three BC variants of a memorised geometry.

`load_dataset` now splits by **group**, defaulting to `family`
(leave-one-family-out). `geometry` and `part` modes exist; `part` exists only to
reproduce the leakage it causes and must never ship a number.

## Labels

| head | source | notes |
|---|---|---|
| `rel_err` | `accuracy_rel_err` vs the case reference | now an independent Gmsh + CalculiX value for 64 cases; 8 remain closed-form |
| `rel_err_rel` | per-case-centred `rel_err` | what the choosers rank on |
| `geo_chamfer`, `geo_p99` | mesh-vs-BRep distance | `p95` collapses to ~0 on conforming meshes and carries no signal |
| `dof`, `mesh_ms`, `solve_ms` | measured | independent of the reference |
| `failure` | `status` / `error` | see below |

**Feasibility and trust are separate, and conflating them was a real defect.**
`accuracy_trusted` is the solve *health gate* — `apps/testlab/main.cpp:2525` sets
it from `health_ok`, and `main.cpp:2562` derives `status` from the same flag, so
`accuracy_trusted == false` is exactly `status == "solve_suspect"` (verified 144
of 144 rows). The loader previously treated that as a *failure*, which
(a) taught the feasibility head the wrong event and (b) discarded those rows'
`n_dof`, `mesh_ms`, `solve_ms` and B-rep distances, none of which depend on the
health gate. Fixing it recovered 1702 → 1778 training and 436 → 504 validation
rows for the cost and geometry heads.

Trust masks the accuracy heads only. Note it is a statement about the *solve*,
not about how close the answer landed: a coarse mesh that is 50 % off is a
trustworthy measurement of a bad action, and it is exactly the signal the
advisor must learn from. Masking on closeness would be selecting on the outcome.

## Known defects in the features

Ten of the original 44 input columns are **constant** across all 3,456 rows and
therefore carry no information:

`diag` (1.0 by construction), **`curved_frac` (1.0 in every row — its formula
`(ntri-12)/ntri` at `apps/testlab/main.cpp:1709` saturates for any real
triangulation)**, `poisson`, `case_poisson`, `case_n_fix_regions`,
`case_n_load_regions`, `p_elevate`, `skin_layers`, `feature_refine`,
`adapt_leb_waves`.

`p_elevate` has since been deleted from the action space — it was redundant, not
merely unvaried: `apps/cli/main.cpp:805` computes
`p_elevate = decision.p_elevate || order >= 2`, the same actuator. The order
vocabulary was trimmed from `[1,2,3,4]` to `[1,2]` for the same reason; orders 3
and 4 were unreachable and the engine warned and downgraded.

The 15 offline descriptors in `geometry_features.csv` were built to replace the
dead geometry signal. They do vary (`curved_area_frac` spans 0.000–0.986), but
see the model card: they did not improve transfer and are now used for OOD
detection instead.

## Action grid

The grid the campaign sweeps is the support of every action column, and the model
must never be asked to extrapolate beyond it. Historical v3 support:
`h_rel {0.12, 0.16, 0.20}` × `order {1,2}` × `mesher {graded_tet, hybrid_zoo}` ×
`adapt_passes {0,1}` × `eta_target {0.005, 0.02, 0.05}` — while the clamp box
allowed `h_rel` down to 0.005, i.e. the policy could emit an action 24× finer
than anything measured.

**81 % of cases (51/63) had the finest offered `h_rel` as their accuracy
optimum** — the grid was saturated at its own edge, which is why a pure-accuracy
objective is degenerate and why evaluation is now budget-constrained and
efficiency-based.

`clamps.json` now carries an explicit `candidate_grid` of exactly the measured
levels, so every action a deployed chooser enumerates is one the model has seen.

## Coverage and gaps

- 63 of 72 v3 cases had ≥3 measured actions; `channel` had 3 usable cases and
  contributes **zero** scorable cases under leave-one-family-out, so macro means
  are over 5 families, not 6. The harness prints a warning saying so.
- Failure rate is ~33 % on the v3 corpus and rises at coarse `h_rel`. On the
  corrected engine the dominant cause changed: a new refusal to alias a feature
  away accounts for 90 of 162 mesh failures, concentrated at coarse rungs.
- Yield *improves* as `h_rel` gets finer — 52 % `mesh_fail` at 0.20, 33 % at
  0.12, 100 % ok at 0.09 — so fine rungs are cheaper per *usable* row than their
  per-row cost suggests. Counterintuitive, and it is why the coarsest rung was
  dropped rather than kept as the cheap option.

## Ethics and risk

No personal data, no human subjects, no scraped third-party content. The
practical risk is a user trusting a numeric prediction on a part outside the
corpus; that is what the OOD gate and the suppression of `predicted_*` fields
behind it exist to prevent.
